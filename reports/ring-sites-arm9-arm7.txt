############ ARM9 (base 0x02000000) ############
_work/arm9.bin
  676952 bytes, load address 0x02000000, ARM/THUMB
  looking for 4070, 4071, 4078, 4079, 4080

  encodability as an ARM data-processing immediate (8 bits rotated by an even amount):
     4070 (0xFE6)  NO   -- must be a literal-pool word
     4071 (0xFE7)  NO   -- must be a literal-pool word
     4078 (0xFEE)  NO   -- must be a literal-pool word
     4079 (0xFEF)  NO   -- must be a literal-pool word
     4080 (0xFF0)  YES  0xFF ror #28

  pass 1 -- immediate fields
       22568 ARM data-processing instructions with an immediate operand
       43310 THUMB instructions carrying a literal
           0 hits

  pass 2 -- 32-bit words in the literal pool
      169238 aligned words scanned
         978 ARM + 9290 THUMB PC-relative loads, 7624 distinct targets
           0 hits

  no 4070 or 4071 or 4078 or 4079 or 4080 anywhere in this image, in either encoding.
  By section 7 of the codec specification that is evidence the
  decoder is not present, not merely that it was not found --
  and on ARM the literal-pool pass is the half that matters,
  because the two cursors cannot be encoded as immediates.

############ ARM7 (base 0x02380000) ############
_work/arm7.bin
  159160 bytes, load address 0x02380000, ARM/THUMB
  looking for 4070, 4071, 4078, 4079, 4080

  encodability as an ARM data-processing immediate (8 bits rotated by an even amount):
     4070 (0xFE6)  NO   -- must be a literal-pool word
     4071 (0xFE7)  NO   -- must be a literal-pool word
     4078 (0xFEE)  NO   -- must be a literal-pool word
     4079 (0xFEF)  NO   -- must be a literal-pool word
     4080 (0xFF0)  YES  0xFF ror #28

  pass 1 -- immediate fields
       11882 ARM data-processing instructions with an immediate operand
        5253 THUMB instructions carrying a literal
           0 hits

  pass 2 -- 32-bit words in the literal pool
       39790 aligned words scanned
        2051 ARM + 242 THUMB PC-relative loads, 1796 distinct targets
           0 hits

  no 4070 or 4071 or 4078 or 4079 or 4080 anywhere in this image, in either encoding.
  By section 7 of the codec specification that is evidence the
  decoder is not present, not merely that it was not found --
  and on ARM the literal-pool pass is the half that matters,
  because the two cursors cannot be encoded as immediates.
